interface Person {
    firstName: string;
    lastName: string;
  }
  const person: Person = {
    firstName: "Giuliana",
    lastName: "Cubilla"
  };
  console.log(`Nombre completo: ${person.firstName} ${person.lastName}`);